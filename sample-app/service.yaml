apiVersion: v1
kind: Service
metadata:
  name: sk060-sample-app
  labels:
    app: sk060-sample-app
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: sk060-sample-app